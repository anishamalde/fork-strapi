import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    flexGrow: 1,
  },
  listItem: {
    paddingBottom: '0',
  },
}));

export default useStyles;
