{
  "name": "fork-strapi-demo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "develop": "lerna run develop --stream",
    "build": "lerna run build --stream",
    "start": "lerna run start --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "engines": {
    "node": ">=14.00"
  }
}
